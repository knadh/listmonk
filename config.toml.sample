[app]
    # Interface and port where the app will run its webserver.
    address = "0.0.0.0:9000"

# Database.
[db]
    host = "db"
    port = 5432
    user = "listmonk"
    password = "listmonk"
    database = "listmonk"
    ssl_mode = "disable"
